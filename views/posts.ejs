<!DOCTYPE html>
<html>

<head>
    <title>게시물 목록</title>
</head>

<body>
    <h1>게시물 목록</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>제목</th>
                <th>작성자</th>
                <th>추천수</th>
                <th>댓글수</th>
                <th>작성일</th>
            </tr>
        </thead>
        <tbody>
            <% posts.forEach(post=> { %>
                <tr>
                    <td>
                        <%= post.id %>
                    </td>
                    <td><a href="/posts/<%= post.id %>">
                            <%= post.title %>
                        </a></td>
                    <td>
                        <%= post.author %>
                    </td>
                    <td>
                        <%= post.likes %>
                    </td>
                    <td>
                        <%= post.comments %>
                    </td>
                    <td>
                        <% if (post.created_at) { %>
                            <%= post.created_at.toLocaleString() %>
                                <% } else { %>
                                    -
                                    <% } %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
    <a href="/posts/new">새 게시물 작성</a>
</body>

</html>